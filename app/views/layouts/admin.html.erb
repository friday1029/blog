<!DOCTYPE html>
<html>
  <head>
    <title>大衛的部落格後台</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_pack_tag 'admins/admin', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render "admins/shared/svgs" %>    
    <%= render "admins/shared/header" %>
    <div class="container-fluid">
      <div class="row">
        <%= render "admins/shared/sidebar" %>
        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>